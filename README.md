# Musaazi Portfolio v2

A sharper Product Designer portfolio for Musaazi Ignatius / Design with Iggy.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Edit content

Most content lives in:

- `data/projects.ts`
- `data/experience.ts`
- `data/testimonials.ts`

Homepage sections live in `components/`.
